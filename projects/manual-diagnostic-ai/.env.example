# Manual-RAG Diagnostic Assistant — Environment Configuration
# ===========================================================

# Ollama server URL (default: local)
OLLAMA_BASE_URL=http://localhost:11434

# Default LLM model (pulled via: ollama pull llama3.1:8b)
LLM_MODEL=llama3.1:8b

# Embedding model (downloaded automatically on first run)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# ChromaDB storage path
CHROMA_PERSIST_DIR=./chroma_db

# Equipment data root
EQUIPMENT_DATA_DIR=./equipment_data

# Chunk size for document splitting (tokens)
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
